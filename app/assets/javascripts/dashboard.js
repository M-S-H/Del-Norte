$(document).ready(function() {
	/*
	$("#dash-links a").click(function() {
		// Change Active Link
		$('.active-dash').removeClass('active-dash');
		$(this).addClass('active-dash');
	
		// Ajax Call
		link = $(this).attr('link');
		$.ajax({
			url: '/dashboard/' + link,
			data: { id: $(this).attr("id") },
			success: function (response) {
				//alert("success");
				var dash = $(response);
				$('#dash-content').append(dash);
			},
			error: function () {
				alert("error");
			}
		}); 
	});
*/
});